<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@color/rpiWhite"
    tools:context=".BallTaps">

    <EditText
        android:id="@+id/trialTaps"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter Number of Taps - Trial"
        android:gravity="center"
        android:backgroundTint="@color/rpiRed"
        android:inputType="number"/>

    <EditText
        android:id="@+id/realTaps"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enter Number of Taps"
        android:gravity="center"
        android:backgroundTint="@color/rpiRed"
        android:inputType="number"/>

    <ImageButton
        android:id="@+id/home"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/home" />

</LinearLayout>